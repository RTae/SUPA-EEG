# Requirements for the Visual Feature Extraction pipeline
# src/model/visual_encoder.py  +  scripts/extract_visual_features.py
#
# Install with:
#   pip install -r requirements-visual-encoder.txt

# ── Core deep learning ───────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0          # image transforms used inside the DataLoader

# ── HuggingFace stack ────────────────────────────────────────────────────────
transformers>=4.35.0          # CLIPVisionModel, CLIPImageProcessor, AutoModel, AutoImageProcessor
huggingface_hub>=0.20.0       # model downloading & caching (pulled in by transformers, pinned for safety)
safetensors>=0.4.0            # fast/safe weight loading format used by modern HF checkpoints

# ── ViT backbone support ─────────────────────────────────────────────────────
timm>=0.9.0                   # required by some HuggingFace ViT variants, including I-JEPA

# ── Image I/O ────────────────────────────────────────────────────────────────
Pillow>=9.0.0                 # PIL.Image used in the image-folder dataset

# ── Logging ──────────────────────────────────────────────────────────────────
loguru>=0.6.0
